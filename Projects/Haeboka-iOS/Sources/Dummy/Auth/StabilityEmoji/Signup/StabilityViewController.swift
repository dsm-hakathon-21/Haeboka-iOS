import UIKit
import Then
import SnapKit
import DesignSystem

open class SignUpRestSixthViewController: BaseRest {

    open override func viewDidLoad() {
        super.viewDidLoad()

        updateWith(self)
    }

    open override func updateWith(_ controller: UIViewController) {
        super.updateWith(controller)
    }
}
